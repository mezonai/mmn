version: "3.8"

services:
  bootstrap_node:
    build: .
    container_name: bootstrap_node
    command: ["./mmn", "bootstrap", "--bootstrap-p2p-port", "9000"]
    ports:
      - "9000:9000"
    networks:
      - mezon

networks:
  mezon: